##Nathan Hinton
##This will load all of the images:

from pygame.image import load

##Load the images. This may be moved to another file later on
chickenImage = load("images/chicken.png")
eggImage = load("images/egg.png")
